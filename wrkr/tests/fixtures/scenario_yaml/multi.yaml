scenarios:
  - name: main
    exec: Default
    executor: constant-vus
    vus: 5
    tags:
      tier: core

  - name: alt
    exec: Alt
    executor: constant-vus
    vus: 2

thresholds:
  http_req_duration{scenario=main}: "p(95)<200"
  checks: ["rate>0.99"]
